#include <stdio.h>

int main() {
    int r=0;int area=0;
    printf("Enter radius of the circle: ");
    scanf("%d",&r);
    area=2*(r*r);
    printf("Area of the Rectangle will be: %d sq. unit",area);
    
    return 0;
}